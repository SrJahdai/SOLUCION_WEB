@model IEnumerable<BEARFLIX.Models.BD.Genero>

@{
    ViewData["Title"] = "Generos";
}
<h1>Administrar Géneros</h1>
<div class="container">
    <form asp-action="CrearGenero" method="post" class="mb-3">
        <div class="form-group">
            <label for="Descripcion">Nuevo Género</label>
            <input type="text" name="Descripcion" id="Descripcion" class="form-control" required />
        </div>
        <button type="submit" class="btn btn-success">Añadir Género</button>
    </form>

    <ul class="list-group">
        @foreach (var genero in Model)
        {
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <span>@genero.Descripcion</span>
                <a class="btn btn-danger" asp-action="EliminarGenero" asp-route-id="@genero.Id">Eliminar</a>
            </li>
        }
    </ul>
    <div class="row my-4">

        <div class="col-12">
            <button type="button" class="btn btn-danger container" id="cancelar" asp-controller="Administrador" asp-action="Index">Cancelar</button>
        </div>
    </div>
</div>
